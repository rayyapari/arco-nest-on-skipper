import { css } from '@emotion/react';
import { GN500, NN0 } from '@tokopedia/nest-color';

export const ctaButton = css`
  background-color: ${GN500};
  border: 0;
  border-radius: 8px;
  color: ${NN0};
  cursor: pointer;
  font-size: 1rem;
  font-weight: bold;
  height: 40px;
  padding: 0.571rem 1.142rem;
`;
